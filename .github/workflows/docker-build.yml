name: Build & Push Docker Image

on:
  # 手动触发：可自定义三个域名（不填则默认本机 localhost）
  workflow_dispatch:
    inputs:
      frontend_domain:
        description: '前端域名 (如 sub.example.com)'
        required: false
        default: ''
      backend_domain:
        description: '后端域名 (如 api.example.com)'
        required: false
        default: ''
      shortlink_domain:
        description: '短链域名 (如 short.example.com)'
        required: false
        default: ''
  # 推送 master 分支时自动构建
  push:
    branches: [master]
    paths:
      - 'frontend/**'
      - 'backend/**'
      - 'shortlink/**'
      - 'Dockerfile'
      - 'Caddyfile'
      - 'entrypoint.sh'
      - 'supervisord.conf'
      - 'docker-compose.yml'
      - '.github/workflows/docker-build.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set domain env vars
        id: domains
        run: |
          FRONTEND_DOMAIN="${{ github.event.inputs.frontend_domain }}"
          BACKEND_DOMAIN="${{ github.event.inputs.backend_domain }}"
          SHORTLINK_DOMAIN="${{ github.event.inputs.shortlink_domain }}"
          # 未输入时默认本机地址
          [ -z "$FRONTEND_DOMAIN" ] && FRONTEND_DOMAIN="localhost"
          [ -z "$BACKEND_DOMAIN" ] && BACKEND_DOMAIN="localhost"
          [ -z "$SHORTLINK_DOMAIN" ] && SHORTLINK_DOMAIN="localhost"
          echo "FRONTEND_DOMAIN=$FRONTEND_DOMAIN" >> $GITHUB_ENV
          echo "BACKEND_DOMAIN=$BACKEND_DOMAIN" >> $GITHUB_ENV
          echo "SHORTLINK_DOMAIN=$SHORTLINK_DOMAIN" >> $GITHUB_ENV
          # 前端调用的后端/短链地址：填了域名用 https，否则本机 http
          if [ "$BACKEND_DOMAIN" != "localhost" ]; then
            echo "BACKEND_URL=https://$BACKEND_DOMAIN" >> $GITHUB_ENV
          else
            echo "BACKEND_URL=http://localhost:25500" >> $GITHUB_ENV
          fi
          if [ "$SHORTLINK_DOMAIN" != "localhost" ]; then
            echo "SHORTLINK_URL=https://$SHORTLINK_DOMAIN" >> $GITHUB_ENV
          else
            echo "SHORTLINK_URL=http://localhost:7999" >> $GITHUB_ENV
          fi
          echo "构建参数: 前端=$FRONTEND_DOMAIN 后端=$BACKEND_DOMAIN 短链=$SHORTLINK_DOMAIN"

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push single image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            zhaoweiwen123/SubHub:${{ github.sha }}
            zhaoweiwen123/SubHub:latest
          build-args: |
            VUE_APP_SUBCONVERTER_DEFAULT_BACKEND=${{ env.BACKEND_URL }}
            VUE_APP_MYURLS_DEFAULT_BACKEND=${{ env.SHORTLINK_URL }}

      - name: Output image
        run: |
          echo "已推送镜像 (Docker Hub):"
          echo "  zhaoweiwen123/SubHub:${{ github.sha }}"
          echo "  zhaoweiwen123/SubHub:latest"
