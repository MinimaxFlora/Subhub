# 单独部署 subconverter 后端（可选）
# 推荐方式：使用根目录 docker-compose.yml 一键部署全家桶
version: "3"

services:
  subconverter:
    build: .
    container_name: kejizero-backend
    ports:
      - "25500:25500"
    restart: always
